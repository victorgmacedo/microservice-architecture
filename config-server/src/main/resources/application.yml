server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/victorgmacedo/config-server-properties
          default-label: main
          search-paths:
          - 'cambio'